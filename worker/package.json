{
  "name": "izy-sensors",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:create": "wrangler d1 create izy-sensors",
    "db:migrate:local": "wrangler d1 migrations apply izy-sensors --local",
    "db:migrate:remote": "wrangler d1 migrations apply izy-sensors --remote",
    "map-env": "node ../scripts/map-env.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.129.1"
  }
}
